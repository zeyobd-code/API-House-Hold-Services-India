[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm prune --production"]

[start]
cmd = "node dist/main.js"

[variables]
PORT = "3000"
HOST = "0.0.0.0"
